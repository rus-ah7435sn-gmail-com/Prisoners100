<UserControl x:Class="PrisonersDilemma.WpfUi.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrisonersDilemma.WpfUi.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Margin="10">
        <TextBlock Text="Number of Prisoners:" />
        <TextBox Text="{Binding NumberOfPrisoners, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
        <TextBlock Text="Max Attempts per Prisoner:" />
        <TextBox Text="{Binding MaxAttempts, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
</UserControl>
